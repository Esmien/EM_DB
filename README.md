Подготовка окружения:

```cp .env.example .env```

Запуск:

```docker compose up --build -d```


Проверка:
http://localhost:8080

СУБД: PostgeSQL

Сервер: `db`

Логин: `admin`

Пароль: `admin`

БД: `em`